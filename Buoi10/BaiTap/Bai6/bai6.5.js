
function findMinNumber(a, b, c) {
    return Math.min(a, b, c);
}

// Kiểm tra ví dụ
const number1 = 8;
const number2 = 3;
const number3 = 12;

const minNumber = findMinNumber(number1, number2, number3);

console.log(`Số nguyên nhỏ nhất là: ${minNumber}`);
